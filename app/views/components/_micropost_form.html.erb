<div>
	<%= form_for(@micropost) do |f| %>
		<%= render 'components/error_messages', object: f.object %>
		<div class="field">
			<%= f.text_area :content, placeholder: "What's happening?", class: "form-control", rows: 3 %>
		</div>
		<%= f.submit "Post", class: "btn btn-large btn-primary" %>
	<% end %>
</div>