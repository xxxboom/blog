<% if @feed_items.any? %>
	<ul class="microposts">
		<%= render partial: 'components/feed_item', collection: @feed_items %>
	</ul>
	<%= will_paginate @feed_items, renderer: BootstrapPagination::Rails %>
<% end %>